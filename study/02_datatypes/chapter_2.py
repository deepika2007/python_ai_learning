spice_mix = set()
print(f"Initial spice mix: {spice_mix}, and id is {id(spice_mix)}")

spice_mix.add("cinnamon")
print(f"After adding cinnamon: {spice_mix}, and id is {id(spice_mix)}")

spice_mix.add("nutmeg")
print(f"After adding nutmeg: {spice_mix}, and id is {id(spice_mix)}")